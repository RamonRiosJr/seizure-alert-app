import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.coquicloud.auraspeaks',
  appName: 'Aura Speaks AI',
  webDir: 'dist',
};

export default config;
